import { StructurizrFile, StructurizrError } from "./types";

const checkers = {};

export const processCheckers = (file: StructurizrFile): StructurizrError[] => {
    let errors: StructurizrError[] = [];
    // for (const checker of checkers ) {
    //     checker(file, errors);
    // }
    return errors;
};